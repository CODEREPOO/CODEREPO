import { TempListFileterPipe } from './temp-list-fileter.pipe';

describe('TempListFileterPipe', () => {
  it('create an instance', () => {
    const pipe = new TempListFileterPipe();
    expect(pipe).toBeTruthy();
  });
});
